 <div class="form-container">
    <div class="form-title">Add New Service</div>
    
    <form method="post" action="{{ path('admin_service_new') }}">
        {{ form_start(form) }}
        
        <!-- Name Field -->
        <div class="form-group">
            {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        </div>

        <!-- Description Field -->
        <div class="form-group">
            {{ form_label(form.description, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
        </div>

        <!-- Price Field -->
        <div class="form-group">
            {{ form_label(form.price, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.price, {'attr': {'class': 'form-control'}}) }}
        </div>
        
        <!-- Submit Button -->
        <div class="button-container">
            <button type="submit">Save</button>
        </div>
        
        {{ form_end(form) }}
    </form>
</div>

<!-- Inline CSS -->
<style>
    /* Apply the background image to the entire page */
    body {
        background-image: url('/imagesS/medical-service.jpg'); /* Correct path */
        background-size: cover; /* Make the background cover the entire page */
        background-position: center; /* Center the image */
        background-repeat: no-repeat; /* Don't repeat the image */
        margin: 0;
        padding: 0;
        height: 100vh; /* Make sure the body takes up the full height of the viewport */
        display: flex;
        justify-content: center;
        align-items: center; /* Center the form vertically */
    }

    /* Form Container */
    .form-container {
        width: 100%;
        max-width: 400px; /* Reduced width */
        padding: 15px; /* Reduced padding */
        background-color: rgba(70, 130, 180, 0.8); /* Darker sky blue with transparency */
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    /* Title */
    .form-title {
        font-size: 22px; /* Smaller title size */
        font-weight: bold;
        margin-bottom: 15px;
        text-align: center;
        color: white; /* White color for the title */
    }

    /* Form Fields */
    .form-group {
        margin-bottom: 15px; /* Reduced space between fields */
    }

    .form-label {
        font-size: 16px; /* Smaller label size */
        font-weight: 700; /* Bold labels */
        color: white; /* White label text for contrast */
    }

    .form-control {
        width: 100%;
        padding: 8px; /* Reduced padding */
        font-size: 14px; /* Smaller font size */
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
    }

    .form-control:focus {
        border-color: #0056b3; /* Darker blue on focus */
        outline: none;
    }

    /* Button Container (to center the button) */
    .button-container {
        text-align: center; /* Center align the button */
    }

    /* Submit Button */
    button[type="submit"] {
        padding: 6px 12px; /* Smaller button with padding */
        background-color: #e74c3c; /* Red button */
        color: white;
        font-size: 12px; /* Smaller font size */
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s;
        margin-top: 10px; /* Space between fields and button */
    }

    button[type="submit"]:hover {
        background-color: #c0392b; /* Darker red on hover */
    }

    /* Responsive Design */
    @media (max-width: 600px) {
        .form-container {
            width: 90%; /* Wider form on smaller screens */
            padding: 10px;
        }

        .form-title {
            font-size: 20px;
        }

        .form-control {
            font-size: 13px;
        }

        button[type="submit"] {
            font-size: 10px;
        }
    }
</style>
